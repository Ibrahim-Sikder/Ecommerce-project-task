import React from 'react';


const DisplayProductDetails = ({product}) => {
    console.log(product)
    return (
        <div>
           
        </div>
    );
};

export default DisplayProductDetails;